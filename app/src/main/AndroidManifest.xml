<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos para la ubicación precisa del usuario -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Manosolidaria"
        tools:targetApi="31">

        <!-- Declaración de actividades de la aplicación -->
        <activity
            android:name=".ui.donante.ListaPropuestasActivity"
            android:exported="false" />

        <activity
            android:name=".ui.donante.PropuestaDetailHostActivity"
            android:exported="false"
            android:label="@string/title_activity_propuesta_detail_host"
            android:resizeableActivity="true"
            tools:targetApi="24" />

        <activity
            android:name=".ui.donante.RegistrarDonacionActivity"
            android:exported="false" />

        <!-- Configuración de Google Maps (si se usa en la app) -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- Declaración de otras actividades -->
        <activity
            android:name=".MapsActivity"
            android:exported="true"
            android:label="@string/title_activity_maps" />

        <activity
            android:name=".ui.login.FormActivity"
            android:exported="false" />

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.HomeActivity"
            android:exported="true"
            android:label="@string/title_activity_home" />

    </application>

</manifest>
